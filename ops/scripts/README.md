This folder contains helper scripts for Claude Code - downloaded automatically from [delegate](https://github.com/derek-larson14/delegate).

Don't delete. Used by slash commands like `/drive`, `/mail`, `/update`, etc.
